node_modules/
.next/
*.tsbuildinfo
out/
build/
dist/
.env
.env.local
.env.*.local
.DS_Store
*.log
.vscode/
.idea/
next-env.d.ts
.vercel

# Staged source assets + raw reference HTML used by scripts/optimize-assets.mjs.
# Kept locally so images can be re-optimized; not part of the shipped app.
_recon/

# Intermediate import artifacts (WooCommerce CSV, parsed/mapped/voice JSON, recon).
# The shipped catalog lives in src/data/pallets.json; these are scratch.
import/

# Local Claude Code tooling config
.claude/
